import Vue from 'vue';
import VueRouter from 'vue-router';
// import header from 'C:/Users/Administrator/webapp/src/components/header/header.vue';

Vue.use(VueRouter);

export default new VueRouter({
  routes: [
    {
      path: '/'
      // name: 'header',
      // component: header
      // name: 'HelloWorld',
      // component: HelloWorld
    }
  ]
});
